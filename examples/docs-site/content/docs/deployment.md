---
title: Deployment
summary: Build, publish, and deploy Static Docs sites.
order: 5
---

# Deployment

Use `build` for production output, `publish` for copying to a target directory, and `gh-deploy` for GitHub Pages.

## Output

:::files
dist/
├── index.html
├── 404.html
└── assets/
    ├── css/
    ├── fonts/
    └── img/
:::

name: Release static-docs

# Watches commits pushed to main.
# Delegates release PR/tag/release creation to the shared Dev-kitx workflow.
#
# CONVENTIONAL COMMITS (determines version bump)
# ───────────────────────────────────────────────
#   feat: ...          → minor bump  (1.1.0 → 1.2.0)
#   fix: ...           → patch bump  (1.1.0 → 1.1.1)
#   feat!: ...         → major bump  (1.1.0 → 2.0.0)
#   BREAKING CHANGE:   → major bump
#   chore/docs/style   → no release PR update
#
# SETUP REQUIRED (one-time)
# ──────────────────────────
# Create a GitHub App and add to repo:
#   vars.APP_ID          — the app's numeric ID
#   secrets.PRIVATE_KEY  — the app's private key (PEM)

on:
  push:
    branches: [main]
    paths-ignore:
      - 'README.md'

permissions:
  contents: write
  pull-requests: write

jobs:
  create-release-pr:
    name: Create Release PR
    uses: Dev-kitx/.github/.github/workflows/github-release.yml@main
    secrets: inherit
